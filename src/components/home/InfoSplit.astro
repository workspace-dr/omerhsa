<section class="py-24 bg-omerhsa-light overflow-hidden">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex flex-col md:flex-row items-center gap-16">
      
      <div class="w-full md:w-1/2 relative">
        <div class="rounded-3xl overflow-hidden shadow-2xl relative z-10 h-[600px]">
          <img 
            src="https://images.unsplash.com/photo-1511895426328-dc8714191300?auto=format&fit=crop&q=80&w=800" 
            alt="Familia protegida" 
            class="w-full h-full object-cover hover:scale-105 transition duration-700" 
          />
        </div>
        <div class="absolute -bottom-10 -left-10 w-40 h-40 bg-omerhsa-primary rounded-full opacity-20 blur-2xl"></div>
        <div class="absolute -top-10 -right-10 w-32 h-32 bg-blue-300 rounded-full opacity-20 blur-2xl"></div>
      </div>

      <div class="w-full md:w-1/2">
        <span class="text-omerhsa-primary font-bold text-sm bg-white border border-omerhsa-primary/20 px-3 py-1 rounded-full shadow-sm">Sin Estrés</span>
        <h2 class="text-4xl md:text-5xl font-bold text-gray-900 mt-4 mb-6 leading-tight">
          Protección simplificada <br/> para tu estilo de vida.
        </h2>
        <p class="text-gray-600 mb-8 text-lg leading-relaxed">
          Nos encargamos de lo complejo para que tú disfrutes de lo importante. Desde la cotización hasta el reclamo, estamos a tu lado.
        </p>

        <ul class="space-y-4">
          {['Asesoría gratuita personalizada', 'Respuesta en menos de 24 horas', 'Acompañamiento legal en reclamos', 'Renovación automática opcional'].map((item) => (
            <li class="flex items-center gap-4 bg-white p-4 rounded-xl shadow-sm border border-transparent hover:border-omerhsa-primary/20 transition-all">
              <span class="flex-shrink-0 w-6 h-6 rounded-full bg-green-100 text-green-600 flex items-center justify-center text-xs font-bold">✓</span>
              <span class="font-medium text-gray-800">{item}</span>
            </li>
          ))}
        </ul>

        <div class="mt-10">
           <a href="/servicios" class="bg-gray-900 text-white px-8 py-4 rounded-xl font-bold hover:bg-omerhsa-primary transition-colors shadow-lg">
             Conocer Proceso
           </a>
        </div>
      </div>

    </div>
  </div>
</section>